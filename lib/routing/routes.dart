/// The code is defining an enumeration called `AppRoutes` with three possible values: `root`, `home`,
/// and `detail`. This enumeration is used to represent different routes in the application.
enum AppRoutes {
  root,
  home,
  detail,
}
